What we need to do for lab 4
-------------------------
All of the moment functions
int computeComponents(inputImage, labeledImage, listOfRegions)
void DeleteSmallComponents(listOfRegions, threshold)
classify regions (menu item)
	* display regions with size between A & B
	*                      orientation
	*                      eccentricity
	*                      mean intensity

Extra credit
	template sorted and unsorted list types
		sorted of type "regionType"
		unsorted of type "PixelType"

========================================

Still want to 
	- add hotkeys for main menu
Low Priority:
	- add Splash screen intro and outro
	- make it so that it can be run from command line only
		ie: main.out -resize 50% image.pgm
hello
hello
